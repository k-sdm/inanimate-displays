.DS_Store
node_modules/
.vercel

# Rhino / Grasshopper / KeyShot working files. These live alongside the
# playground but run to hundreds of megabytes each, and have no business in a
# repo that serves one HTML file.
*.3dm
*.3dmbak
*.rhl
*.bip
*.gh
